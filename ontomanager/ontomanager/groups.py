"""
This module defines the groups, which users can belong to.
Groups and users are configured in the config.ini.
"""

GROUP_VIEWING  = 'group:viewing'
GROUP_EDITING  = 'group:editing'
GROUP_QUERYING = 'group:querying'
